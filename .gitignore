# Secrets — never push to GitHub
.env

# Python cache
__pycache__/
*.pyc
*.pyo

# Local database (managed by GitHub Actions cache, not git)
bot4_database.db
bot_data.json

# Local number stock files (managed by GitHub Actions cache, not git)
country_stocks/
